#include "VersionSkipList.h"

Version* VersionSkipList::find(Timestamp ts) {
    int i;

}


int VersionSkipList::add(Timestamp ts, Version* v) {
}

int VersionSkipList::remove(Timestamp ts) {
    return 0;
}
